{
  "name": "pushup-tracker",
  "version": "1.0.0",
  "description": "Serverless API for Pushup Tracker via Vercel and Supabase",
  "main": "api/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1"
  }
}
